{
  "name": "go-react-redux-elm",
  "version": "0.1.0",
  "summary": "I Implement the same Go Board with Vanilla React, React + Redux & Elm",
  "scripts": {
    "build": "elm-make src/Main.elm --output=app.js",
    "build:watch": "chokidar '**/*.elm' -c 'elm-make src/Main.elm --output=app.js'",
    "prepublish": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": "https://github.com/ajhyndman/go-react-redux-elm.git",
  "license": "MIT",
  "devDependencies": {
    "chokidar-cli": "^1.2.0",
    "elm": "^0.17.0",
    "materialize-css": "^0.97.6"
  }
}
